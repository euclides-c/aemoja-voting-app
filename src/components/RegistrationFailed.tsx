import React from 'react';
import { Result } from 'antd';

const RegistrationFailed = () => {
	return (
		<div>
			<Result status='error' title='Ocorreu um erro durante o registo' />;
		</div>
	);
};

export default RegistrationFailed;
